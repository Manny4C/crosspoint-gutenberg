version: 2
updates:
  # Keep the GitHub Actions used by CI / release workflows up to date.
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    commit-message:
      prefix: ci
